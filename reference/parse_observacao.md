# parse_observacao

Parseia um `<item>` de `<Observacoes>`.

## Usage

``` r
parse_observacao(nd_obs)
```

## Arguments

- nd_obs:

  Um nó `xml2` de `<item>`.

## Value

Um tibble de 1 linha com `Descricao`.
