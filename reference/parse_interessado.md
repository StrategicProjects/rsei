# parse_interessado

Parseia um `<item>` de `<Interessados>`.

## Usage

``` r
parse_interessado(nd_int)
```

## Arguments

- nd_int:

  Um nó `xml2` de `<item>`.

## Value

Um tibble de 1 linha com `Sigla` e `Nome`.
