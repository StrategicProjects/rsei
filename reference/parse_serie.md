# parse_serie

Parseia uma estrutura `Serie`.

## Usage

``` r
parse_serie(node)
```

## Arguments

- node:

  Nó `xml2` da `Serie`.

## Value

Um tibble de 1 linha com `IdSerie`, `Nome`, `Aplicabilidade`.
