# parse_unidade

Parseia uma estrutura `Unidade` (filho direto ou `<item>`).

## Usage

``` r
parse_unidade(node)
```

## Arguments

- node:

  Nó `xml2` da `Unidade`.

## Value

Um tibble de 1 linha com os campos da unidade.
