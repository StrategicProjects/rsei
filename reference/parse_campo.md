# parse_campo

Parseia uma estrutura `Campo` (formulário).

## Usage

``` r
parse_campo(node)
```

## Arguments

- node:

  Nó `xml2` do `Campo`.

## Value

Um tibble de 1 linha com `Nome` e `Valor`.
