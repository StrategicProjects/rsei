# parse_assinatura

Parseia uma estrutura `Assinatura`.

## Usage

``` r
parse_assinatura(node)
```

## Arguments

- node:

  Nó `xml2` da `Assinatura` (filho direto ou `<item>`).

## Value

Um tibble de 1 linha com os campos da assinatura.
