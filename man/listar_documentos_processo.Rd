% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listas.R
\name{listar_documentos_processo}
\alias{listar_documentos_processo}
\title{listar_documentos_processo}
\usage{
listar_documentos_processo(
  protocolo_procedimento,
  config = sei_config(),
  consultar = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{protocolo_procedimento}{Character. Número do processo.}

\item{config}{Um objeto \code{\link[=sei_config]{sei_config()}}.}

\item{consultar}{Logical. Se \code{TRUE}, consulta cada documento encontrado com
\code{\link[=consultar_documentos]{consultar_documentos()}} e anexa os detalhes (série, data, unidade
elaboradora, etc.). Faz uma chamada por documento.}

\item{verbose}{Logical.}
}
\value{
Um \code{tibble} com uma linha por documento: \code{documento} (número),
\code{DataHora}/\code{SiglaUnidade}/\code{SiglaUsuario}/\code{NomeUsuario}/\code{Andamento} da
primeira menção (a geração). Com \code{consultar = TRUE}, inclui também as
colunas de \code{\link[=consultar_documento]{consultar_documento()}}.
}
\description{
Reconstrói a lista de documentos de um processo a partir dos seus andamentos.

O Web Service do SEI \strong{não possui} uma operação nativa para listar os
documentos de um processo; portanto esta função recupera a linha do tempo com
\code{\link[=listar_andamentos_completo]{listar_andamentos_completo()}} e extrai os números de documento mencionados
nas descrições (ex.: "Gerado documento ... 84230597"). É uma heurística: o
resultado depende do texto dos andamentos e pode não captar 100\% dos casos.
}
\examples{
\dontrun{
  docs <- listar_documentos_processo("12.1.000000077-4", config = sei_config())
  docs[, c("documento", "DataHora", "SiglaUnidade", "NomeUsuario")]

  # com detalhes (série, data, etc.)
  listar_documentos_processo("12.1.000000077-4", consultar = TRUE)
}

}
