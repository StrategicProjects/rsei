% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core.R
\name{sei_call}
\alias{sei_call}
\title{Chamada Genérica a um Web Service do SEI}
\usage{
sei_call(
  operation,
  params = list(),
  config = sei_config(),
  soap_action = "SeiAction",
  ns_prefix = "sei",
  ns_uri = "Sei",
  verbose = FALSE
)
}
\arguments{
\item{operation}{Character. Nome da operação SOAP.}

\item{params}{Lista nomeada de parâmetros (ver \code{\link{sei_build_envelope}}).}

\item{config}{Um objeto \code{\link{sei_config}}.}

\item{soap_action}{Character. Valor do cabeçalho \code{SOAPAction}.}

\item{ns_prefix, ns_uri}{Namespace da operação (ver \code{\link{sei_build_envelope}}).}

\item{verbose}{Logical. Se \code{TRUE}, imprime o envelope enviado e a resposta.}
}
\value{
Um \code{xml2::xml_document} com a resposta SOAP.
}
\description{
Monta o envelope SOAP para \code{operation} com \code{params}, envia via
\code{httr2} e devolve a resposta como \code{xml_document}. Trata erros HTTP
e \code{SOAP Fault} (o SEI retorna falhas em HTTP 500 com \code{faultstring}).
}
\examples{
\dontrun{
  doc <- sei_call("listarUnidades",
    params = list(SiglaSistema = "HORTENSIAS",
                  IdentificacaoServico = "chave"),
    config = sei_config())
}

}
