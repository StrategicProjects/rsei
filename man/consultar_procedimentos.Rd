% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consultaProcedimento.R
\name{consultar_procedimentos}
\alias{consultar_procedimentos}
\title{consultar_procedimentos}
\usage{
consultar_procedimentos(
  protocolos,
  config = sei_config(),
  parar_em_erro = FALSE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{protocolos}{Vetor de números de processo (ex.: \code{c("12.1.0001-1", ...)}).}

\item{config}{Um objeto \code{\link[=sei_config]{sei_config()}}.}

\item{parar_em_erro}{Logical. Se \code{TRUE}, interrompe na primeira falha; se
\code{FALSE} (padrão), registra o erro na coluna \code{erro} e segue.}

\item{verbose}{Logical. Repassado a \code{\link[=consultar_procedimento]{consultar_procedimento()}}.}

\item{...}{Demais argumentos repassados a \code{\link[=consultar_procedimento]{consultar_procedimento()}} (ex.:
os sinalizadores \verb{sin_retornar_*}). Não use \code{raw} aqui.}
}
\value{
Um \code{tibble} com uma linha por processo (colunas \code{protocolo} e \code{erro}
além das de \code{\link[=consultar_procedimento]{consultar_procedimento()}}). Linhas com erro têm \code{NA} nas
demais colunas.
}
\description{
Consulta vários processos de uma vez e empilha os resultados em um único
\code{tibble}. Cada protocolo é consultado com \code{\link[=consultar_procedimento]{consultar_procedimento()}} e o
resultado recebe uma coluna \code{protocolo} (o número consultado) e uma coluna
\code{erro} (\code{NA} em caso de sucesso; a mensagem do erro caso a consulta falhe).
Por padrão um protocolo com erro não interrompe o lote.
}
\examples{
\dontrun{
  protos <- c("0000000000.000001/2020-11", "0000000000.000003/2020-33")
  tudo <- consultar_procedimentos(protos, config = sei_config())
  tudo[, c("protocolo", "Especificacao", "NivelAcessoGlobal", "erro")]
}

}
