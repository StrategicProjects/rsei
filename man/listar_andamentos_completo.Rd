% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listas.R
\name{listar_andamentos_completo}
\alias{listar_andamentos_completo}
\title{listar_andamentos_completo}
\usage{
listar_andamentos_completo(
  protocolo_procedimento,
  config = sei_config(),
  tarefas = 1:200,
  sin_retornar_atributos = "N",
  ordenar = TRUE,
  raw = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{protocolo_procedimento}{Character. Número do processo.}

\item{config}{Um objeto \code{\link[=sei_config]{sei_config()}}.}

\item{tarefas}{Vetor de identificadores de tarefa a recuperar (padrão
\code{1:200}, que abrange as tarefas internas do SEI).}

\item{sin_retornar_atributos}{"S" ou "N".}

\item{ordenar}{Logical. Se \code{TRUE} (padrão), ordena por \code{DataHora} (mais antigo
primeiro).}

\item{raw, verbose}{Logical.}
}
\value{
Um \code{tibble} de andamentos ordenado por data/hora (ver
\link{parse_andamento_item}).
}
\description{
Conveniência sobre \code{\link[=listar_andamentos]{listar_andamentos()}} que recupera a \strong{linha do tempo
completa} de um processo e a ordena cronologicamente. Como a operação
\code{listarAndamentos} do SEI exige um filtro de tarefas, esta função usa por
padrão um intervalo amplo de identificadores de tarefa (que cobre as tarefas
internas do SEI: geração, documentos, assinaturas, envio, recebimento,
conclusão, blocos, etc.).
}
\examples{
\dontrun{
  linha <- listar_andamentos_completo("12.1.000000077-4", config = sei_config())
  linha[, c("DataHora", "Descricao", "SiglaUnidade", "NomeUsuario")]
}

}
